workflows:
  android-build:
    name: Build Android APK
    max_build_duration: 15
    environment:
      flutter: stable
      java: 17
    scripts:
      - ./inventory-app/gradlew clean assembleDebug
    artifacts:
      - inventory-app/app/build/outputs/**/*.apk
    publishing:
      email:
        recipients:
          - andrey@ppsspb.ru
